{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "uvicorn app.main:app --app-dir backend --host 0.0.0.0 --port ${PORT:-8080}",
        "restartPolicyType": "ON_FAILURE"
    }
}